# 数据源：mock（默认，前端本地数据）| real（走 /api 真实接口）
VITE_API_MODE=mock

# 真实接口地址；留空则使用 /api，由 Vite dev server 代理到 NestJS
VITE_API_BASE_URL=

# mock 请求模拟耗时（毫秒），用于验证 loading 态
VITE_MOCK_DELAY=260

# 每 N 次请求模拟一次失败，用于验证错误态与重试；0 表示不模拟
VITE_MOCK_FAILURE_EVERY=0
