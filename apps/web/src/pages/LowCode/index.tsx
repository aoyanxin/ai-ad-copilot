import { PagePlaceholder } from '../../components/PagePlaceholder';

export function LowCodePage() {
  return (
    <PagePlaceholder
      title="LowCode"
      description="低代码 Schema 渲染：Day4 接入 JSON Schema 到组件的映射。"
    />
  );
}
